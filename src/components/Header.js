import React from 'react';
import './Header.css';

const Header = () => {
	return (
		<div className="Header--container">
			<h1 className="Header--pageTitle">Restaurant Voting System</h1>
			<p>Where are we going?</p>
		</div>
	);
};

export default Header;